# Memory — 2026-03-27

## LangLearn — Công việc hôm nay (02:45 → 05:10)

### Fixes đã deploy
1. **Homepage navbar auth** — `page.tsx` hardcode navbar riêng → tách thành server component + `HomePageClient.tsx`. Sau login click logo về `/` navbar hiện đúng.
2. **Blog render Markdown** — content lưu dạng Markdown nhưng render thô → cài `marked`, thêm CSS `prose-langlearn` (heading, table, blockquote, list, code).
3. **Blog upload từ file .txt/.md** — `/api/admin/blog/upload`, drag-drop UI, tạo bài dưới dạng nháp. `BlogActionsClient` có Publish/Ẩn/Xóa.
4. **POST /api/admin/lessons thiếu** → đã thêm route.
5. **`/courses/[id]` redirect login khi guest** — bỏ `redirect('/login')` trong server component, guest xem được.
6. **Admin sidebar mobile** — trước: sidebar 240px cố định chiếm màn hình. Fix: ẩn mặc định trên mobile, hamburger ☰ toggle, overlay đóng khi click ngoài. Desktop giữ nguyên.

### Đồng bộ Chó
- Gửi context qua Gateway API (`/v1/chat/completions`)
- Chó đã `git pull` commit `09ab6c6`, `d5e4d00`, `f9fe07e`
- Chó đổi `bind=loopback` → Chiến không ping được qua Tailscale nữa
- Kênh đồng bộ chính: `logs/DEVLOG.md` trong repo + file `workspace/giao-tiep.md`

### DB state hiện tại
- **Courses:** 0 (Gia Bảo xóa hết)
- **Lessons:** 0 (xóa hết)
- **Exercises:** 0 (xóa hết)
- **Blog posts:** 10 bài (published), do seed + 3 bài cũ

### Commits hôm nay
| Hash | Nội dung |
|------|---------|
| b938c6e | fix homepage navbar auth |
| 09ab6c6 | blog markdown render + prose CSS |
| f2c6222 | blog upload .txt, BlogActionsClient |
| d5e4d00 | POST /api/admin/lessons, fix middleware |
| ebda5c7 | course detail guest access, seed exercises (đã xóa hết) |
| f9fe07e | admin sidebar mobile responsive |

### Giao tiếp Chó
- Không dùng `sessions_send` được với Chó
- Dùng `exec curl POST http://100.73.204.7:18789/v1/chat/completions` (nhưng bind=loopback nên timeout)
- File `workspace/giao-tiep.md` = kênh async Chiến ↔ Chó
- File `projects/langlearn/logs/DEVLOG.md` = log kỹ thuật chung

### Bài học kỹ thuật
- Next.js: `'use client'` page không đọc được cookie → phải tách server component wrapper
- Middleware PUBLIC_PATHS dùng `startsWith` nhưng server component vẫn có thể tự `redirect()` → phải fix cả 2 chỗ
- Admin layout cần `AdminSidebarProvider` wrap để Header và Sidebar share state
- `createContext` trong file component thì cả Provider và consumer phải import từ cùng file


## Seed A1 Full — 13:32

### Tình trạng
- Subagent #1 (seed A1 full): timeout sau 10 phút — chỉ viết 4 dòng rỗng vào file
- Subagent #2 (seed A1 full, approach Python gen): đã spawn lúc 13:23, chờ kết quả
- File hiện tại: `/home/tmc/projects/langlearn/prisma/seed-a1-full.ts` — gần trống (4 dòng)
- Subagent #3 (label: seed-a1-full): spawned lúc ~13:32, dùng Python script để gen TypeScript file

### Task còn lại
- Seed 10 lessons × 20+ exercises = 200+ exercises vào DB LangLearn
- Sau seed xong: commit + push origin main
- Course A1 đã bị Gia Bảo xóa khỏi DB → cần chạy `npx prisma db seed` trước rồi mới chạy seed-a1-full.ts

### Lưu ý kỹ thuật seed
- Schema: NO @@unique([courseId, order]) → dùng findFirst + update/create pattern
- ts-node command: `npx ts-node --compiler-options '{"module":"commonjs"}' prisma/seed-a1-full.ts`
- Course lookup: `findFirst({ where: { title: { contains: 'A1' } } })`

---

## PROJECT SNAPSHOT — LangLearn (cuối ngày 27/03, ghi lúc 03:28 28/03)

### 🌐 Live URLs
- **App:** http://tuhoctiengduc.giabaobooks.vn:12431
- **IP direct:** http://118.70.49.57:12431
- **Admin:** /admin (admin@langlearn.vn / Admin@2026)

### 📁 VPS Paths
- **Project:** `/home/tmc/projects/langlearn`
- **PM2:** `pm2 restart langlearn`
- **Nginx config:** `/etc/nginx/sites-available/langlearn`
- **DB:** PostgreSQL `langlearn` db, user `langlearn_user`

### 🗃️ DB State (cuối ngày 27/03)
- **Courses:** 1 (Tiếng Đức A1)
- **Lessons:** 10 bài (order 1-10)
- **Exercises:** 200 (20/bài × 10 bài, 5 loại đủ cả)
- **Blog posts:** 10 bài published
- **Users:** 1 admin (admin@langlearn.vn)

### 📦 Tính năng đang có (LIVE)
| Module | URL | Ghi chú |
|--------|-----|---------|
| Landing | / | Demo 5 câu random, CTA |
| Courses | /courses | List + detail, guest xem được |
| Practice | /practice/[lessonId] | 5 loại bài tập, chấm điểm |
| Dashboard | /dashboard | Heatmap, streak, progress |
| Leaderboard | /leaderboard | Tuần/All-time, top 20 |
| Blog | /blog | Search, pagination, markdown |
| Store | /store | Landing bán sách |
| Roadmap | /roadmap | A1→B2 |
| Profile | /profile | Đổi tên/mật khẩu |
| Forgot password | /forgot-password | Token 15 phút |
| Admin panel | /admin | Full CRUD courses/lessons/exercises/blog/users |
| Import Excel | /admin/lessons/[id] | Upload .xlsx → parse → import |

### 🔧 Stack
- Next.js 14 App Router + TypeScript
- PostgreSQL + Prisma ORM
- JWT httpOnly cookie + Refresh Token Rotation (jose, Edge Runtime)
- Tailwind CSS + shadcn/ui
- PM2 (fork mode) + Nginx reverse proxy
- GitHub Actions CI/CD → SSH → VPS

### 🚀 Deploy workflow
```bash
# Manual rebuild khi cần:
cd /home/tmc/projects/langlearn
git fetch && git reset --hard origin/main
rm -rf .next && npm run build
pm2 restart langlearn
```

### ⚠️ Tồn đọng / TODO
1. **Port :12431** vẫn lộ trong URL → cần Cloudflare Tunnel hoặc NAT port 80
2. **SSL/HTTPS** chưa có → cần sau khi giải quyết port
3. **Seed Excel** — Gia Bảo đang test import file mẫu
4. **A2/B1/B2** courses chưa có — cần seed hoặc nhập thêm
5. **Email verification** đăng ký chưa có
6. **Rate limiting** login: có (5 lần/15 phút) nhưng chưa test kỹ
7. **Backup DB** chưa setup tự động

### 🐛 Known bugs còn lại (chưa fix)
- Chưa phát hiện thêm bug nào sau các fix hôm nay

### 📝 Cách thêm bài tập mới
**Option 1 — Excel import (nhanh):**
1. Vào Admin → Courses → Lesson → "Tải file mẫu"
2. Điền data vào file .xlsx (mỗi sheet 1 loại, options/words ngăn bởi `|`)
3. Upload → Import

**Option 2 — Seed script:**
```bash
cd /home/tmc/projects/langlearn
# Edit prisma/seed-a1-full.ts hoặc tạo file mới
npx ts-node --compiler-options '{"module":"commonjs"}' prisma/seed-a1-full.ts
```

**Option 3 — Admin UI:**
Vào /admin/lessons/[id] → form "Thêm bài tập mới" → điền từng câu

### 🔑 Credentials
- **Admin:** admin@langlearn.vn / Admin@2026
- **GitHub PAT:** [GITHUB_PAT]
- **Repo:** https://github.com/GiaBao72/langlearn

### 💡 Patterns Next.js đã học (quan trọng)
- Sau login/logout → dùng `window.location.href` (full reload), KHÔNG `router.push()`
- Navbar auth: server component đọc cookie → truyền xuống NavbarClient
- pm2 KHÔNG load `.env` → hardcode secrets trong `ecosystem.config.js`
- `jose` thay `jsonwebtoken` (Edge Runtime compatible)
- Cookie `Secure` chỉ set khi `NEXTAUTH_URL` starts with `https`

---

## TODO từ Gia Bảo (28/03 04:26)
- [ ] Avatar user — dùng DiceBear auto-generate từ tên/email. Không cần DB migration. Hiện ở Navbar, Dashboard, Leaderboard, Profile.
