# LANGLEARN PROJECT - SESSION LOG
# Ngày: 2026-03-25
# Team: Nhà Vua (Gia Bảo) + Chó 🐶 (PC Win11) + Gemini 🤖 + Chiến 🕺 (VPS)
# ====================================================================

## TIMELINE

### 01:34 - Session bắt đầu
- Bàn phân công: Chó làm hàng ngày, Chiến backup/emergency
- Setup webhook server port 9000 (brandpage-landing monitor, 5phut-page deploy)

### 02:18 - TECH LEAD KICK OFF
- Nhà Vua giao xây website học ngoại ngữ toàn diện
- Stack được duyệt: Next.js 14 + PostgreSQL + Prisma + JWT httpOnly cookie
- DB Schema thiết kế: User, RefreshToken, Course, Lesson, Exercise, UserProgress, BlogPost

### 02:45 - Clone & Setup VPS
- Clone langlearn repo từ GitHub
- Tạo DB + user PostgreSQL
- Tạo .env với JWT secrets
- npm install + npx prisma migrate dev
- pm2 start, nginx config

### 04:45 - Backend APIs
- /api/auth: login, register, logout, refresh
- /api/lessons/[id]/exercises: GET với progress
- /api/exercises/[id]/submit: POST với scoring engine
  * MULTIPLE_CHOICE/FILL_BLANK/FLASHCARD: full/no points
  * DICTATION: partial score theo % từ đúng
  * SORT_WORDS: full/no points
  * Keep best score policy
- /api/blog: CRUD public + admin
- /api/admin/courses: full CRUD + lesson count
- /api/admin/lessons: full CRUD + exercise count
- /api/admin/exercises: full CRUD

### 04:52 - Seed Data
- prisma db seed: 1 course Tiếng Đức A1
- Lesson: "Bài 1 - Tự giới thiệu bản thân"
- 5 exercises: FLASHCARD, FILL_BLANK, MULTIPLE_CHOICE, DICTATION, SORT_WORDS

### 06:00 - UI Bug Fixes (nhiều vòng)
- Dark mode → Light mode force
- MultipleChoice useRef fix (closure bug)
- Light mode palette: #2563EB primary, #F5A623 accent

### 06:35 - GitHub Actions CI/CD
- Setup SSH deploy key: ~/.ssh/github_deploy
- GitHub Secrets: VPS_HOST, VPS_PORT, VPS_USER, VPS_SSH_KEY
- Auto-deploy: push main → SSH VPS → git reset --hard → npm run build → pm2 restart

### 07:00 - Feature Additions (Chó + Gemini)
- Landing page v2: stats bar, 6 features, bottom CTA
- Full responsive design mobile-first
- Shared Navbar với links
- Courses search + filter
- Roadmap page, Store/Book page
- Blog với typography styles
- Dashboard với heatmap + streak
- Vietnamese slugify fix

### 07:49 - Critical Bug: /practice redirect loop
- Root cause: server component tự gọi redirect('/login') → bypass middleware
- Fix: bỏ auth guard trong PracticePage, user = null là guest OK
- Commit: dfb2d7b

### 09:00 - Admin UI (Chó)
- Admin lesson edit page /admin/lessons/[id]
- Course detail với lesson links
- Exercise management per lesson
- Admin blog editor với Word import (mammoth.js)

### 11:06 - Auth/Navbar Bug
- Root cause: router.push() sau login = client-side nav → server Navbar không re-render
- Fix: window.location.href cho full page reload
- Split Navbar: server component (đọc cookie) + NavbarClient (logout button)

### 11:13 - Build Error: Turbopack
- Root cause: Navbar (async server component) import vào 'use client' pages
- Fix:
  * Navbar dùng jose thay jsonwebtoken (Web Crypto compatible)
  * store/page.tsx: bỏ 'use client', tách FaqAccordion client component
  * DashboardClient + CoursesClient: bỏ Navbar, move lên server page wrapper

### 11:40 - VPS Hardening
- PM2: fork mode, port 3000, max_memory_restart 512M
- Nginx: đã có, listen :1234 → proxy :3000
- DB Backup: cron 2AM hàng ngày, giữ 7 ngày, /home/tmc/backups/langlearn/

### 16:55 - shadcn/ui Migration (ongoing)
- Chó đang migrate tất cả pages sang shadcn/ui components

## BUGS FIXED (danh sách đầy đủ)

1. jsonwebtoken không chạy Edge Runtime → dùng jose
2. Cookie Secure=true trên HTTP → browser block → check NEXTAUTH_URL.startsWith("https")
3. pm2 không load .env → hardcode trong ecosystem.config.js
4. Server component tự redirect() bypass middleware public paths
5. router.push() sau login = client-side → Navbar không re-render → dùng window.location.href
6. Navbar async server component không thể import trong 'use client' component tree
7. MultipleChoice closure bug với useRef fix
8. GitHub Actions continue-on-error mask failures
9. VPS serving stale build vì Actions không rebuild → fix workflow thêm npm run build

## COMMITS (70 total)
- First: 2657a82 - feat: initial project setup
- Last:  23b9ce1 - chore: pm2 fork mode port 3000

## VPS STATE (end of day)
- PM2: langlearn online, fork mode, port 3000
- Nginx: :1234 → :3000
- RAM: 1.8G / 11G
- Disk: 18G / 98G
- DB Users: 4 (admin@langlearn.vn + test + 2 others)
- Backup: /home/tmc/backups/langlearn/, cron 2AM daily
